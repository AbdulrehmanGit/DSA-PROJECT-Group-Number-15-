the code is easy to implement 
1-you have to make sure that you have imported random(it is already imported in the code but if in any case if random is missing then import it) 
2-create an object of class SkiplistSSet()
3-add some elements in to the list by using the add(x) function
4-display the elements in the list by using displayList() function
5-remove some elements from the list remove(x) function